import Parallax from "../../../features/Parallax";
import { Container } from './Empresa.Styled';

const Empresa = () => {
    return (
        <Container>
            <Parallax />
        </Container>
    );
};

export default Empresa;